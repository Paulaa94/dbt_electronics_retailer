{% snapshot snap_customers %}
{{
    config(
        target_schema='snapshots',
        unique_key='customer_key',
        strategy='check',
        check_cols=['name', 'gender', 'birthday', 'state', 'country', 'continent']
    )
}}

select * from {{ ref('stg_customers') }}

{% endsnapshot %}
